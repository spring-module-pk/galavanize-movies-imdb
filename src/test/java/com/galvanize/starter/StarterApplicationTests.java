package com.galvanize.starter;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

//@SpringBootTest
class StarterApplicationTests {

	@Test
	void contextLoads() {
		// This test is to ensure that the StarterApplication class is present and can be loaded.
		// It does not need to contain any assertions, as the context loading itself is the validation.
		// If the application context fails to load, this test will fail.
		// The actual logic of the application is tested in other tests.
		// This is a placeholder for future tests that may be added.
	}

}
